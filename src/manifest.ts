// The deployed-artifact vocabulary is core's, since a host reads a sidecar without knowing which
// plugin manager wrote it. Re-exported here so this repo's imports name one module.
// @ts-ignore - generated bundle, no .d.ts
export { DEPLOYED_SUFFIXES, readCloneManifest, deployedIdFor, syncManifestSidecar } from "@intisy-ai/basekit";
